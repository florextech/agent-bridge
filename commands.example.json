{
  "commands": {
    "test": "pnpm test",
    "build": "pnpm -r build",
    "deploy": "docker compose up -d",
    "lint": "pnpm -r lint",
    "status": "git status",
    "log": "git log --oneline -5"
  },
  "default": "echo"
}
